1.)Create a class and a static function which takes classId as argument and returns all names of the
students in that class as a list.

2.)Create a function which returns a map where key is class and value is set of studentId in it.

3.)Create a function which take two dates as argument and return number of working days between two
dates excluding both days. (Saturday and Sunday is non-working days)

4.)Create a class which show the list of sobjects exists in org.